function addition(a, b) {
    return a + b;
  }
  
  function substraction(a, b) {
    return a - b;
  }
  
  //to export the these functions to the index files
  
  exports.add = addition;
  exports.sub = substraction;