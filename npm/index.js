//to import from the helper file

const global = require("./helper")

console.log(global.add(3,3));
console.log(global.sub(10,5));

// to run this inside the package.json in the scripts we have write the cmd start: "node file_Name"
//node we have run the node with file index 
