const global = require("os");
//require is for importing 

let platForm = global.platform()
//planform() is inbuild function that checks of os plat form
console.log(platForm);

// to check the operating system we have use code modules inbuilt package os